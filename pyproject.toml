[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "netops-commander-llm"
version = "0.1.0"
description = "On-prem NetOps LLM scaffold aligned to PRD v2.0.0"
readme = "README.md"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
